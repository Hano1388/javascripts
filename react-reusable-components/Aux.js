// this is a higher order component, name it Auxilliary.js

const Auxiliary = props => props.children;

export default Auxilliary;

// usage: just import it to your component and wrap jsx with it

// NOTE: it's a convention to name it Aux but, because Aux is a keyword in windows, we use Auxiliary
